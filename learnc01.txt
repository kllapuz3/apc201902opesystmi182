#include <stdio.h>

int main()
{
    int a, b, c;
    printf("Enter X coordinate:");
    scanf("%d", &a);
    printf("Enter Y coordinate:");
    scanf("%d", &b);
    printf("Your Coordinates: %d, %d \n", a, b);
    return 0;
}